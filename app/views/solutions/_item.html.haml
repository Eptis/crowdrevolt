- if item.item_type == "youtubefilm"
  .videoWrapper=raw item.youtubeembedlink
- if item.item_type == "afbeelding"
  .image=image_tag item.image.url(:front)
- if item.item_type == "tekst"
  .text=raw simple_format(clean(item.body))