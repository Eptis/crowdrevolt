- model_class = Admin
.page-header
  %h1=t '.title', :default => model_class.model_name.human.pluralize
%table.table
  %thead
    %tr
      %th= model_class.human_attribute_name(:id)
      %th= model_class.human_attribute_name(:username)
      %th= model_class.human_attribute_name(:email)
      %th= model_class.human_attribute_name(:skypename)
      %th= model_class.human_attribute_name(:created_at)
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
  - @admins.each do |admin|
    %tr
      %td= admin.id
      %td= admin.username
      %td= admin.email
      %td= admin.skypename
      %td= admin.created_at
      %td
        = link_to t('.edit', :default => t("helpers.links.edit")), [:edit, :admin, admin], :class => 'btn btn-mini'
        = link_to t('.destroy', :default => t("helpers.links.destroy")), [:admin, admin], :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini btn-danger'


= link_to t('.new', :default => t("helpers.links.new")), [:new, :admin, :admin], :class => 'btn btn-primary'

/ = will_paginate @admins