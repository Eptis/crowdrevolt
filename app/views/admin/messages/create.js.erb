<% broadcast ("/#{@fayechannel.to_s}") do %>
  $("#chatBox").append("<%= escape_javascript render(@message) %>");
  var chat = document.getElementById('chatBox');
  chat.scrollTop = chat.scrollHeight;
<%end%>
$("#new_message .message_clear").val("");
